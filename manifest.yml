---
buildpack: ruby_buildpack
services:
  - rails-postgres
env:
  RACK_ENV: production
  RAILS_ENV: production
  pong_location: Beaverton
applications:
  - name: beaverton-pong
    command: bundle exec rake db:migrate && bundle exec rails s -p $PORT
